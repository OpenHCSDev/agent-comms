# Core PR #1 merge tracker — 2026-09-24

**Published draft head:** `ceb87f52850875f27fd0b3d2ec6c3bfb57bbaa0c`. **CI:** [run 35958812645](https://github.com/OpenHCSDev/agent-comms/actions/runs/35958812645) queued at this update. The preceding `9d2e828` run passed 10/11 jobs; macOS Python 3.13 failed two backend input-provenance assertions. This is a status ledger, **not** an independent review verdict. Draft publication and merge never authorize production, automatic retries, `SILENT`, or paid/tool-capable work. Do not replay an uncertain input/provider attempt.

The obsolete local tracker is preserved as `/var/tmp/ac-pr1-merge-tracker-legacy-20260924.md` (SHA-256 `523dbd8c7117d1bafe042875bfd4a0d76d02295596b6ad5594d88152f315b589`); its `ed9b4a1` owner map and verdicts are not current-head evidence.

| Gate / current evidence | Owner (@thread) | Exact next close condition |
| --- | --- | --- |
| Saved-session `/compact`: ACP route, runner and bridge published at `dd6a76a`; pinned no-provider CI fixture at `9d2e828`; Pi session-file + parent fsync and denial tests at `9ba0ea1`. Runner now `e508bb99…`, not old `667d1abd…` (old independently **NON-CLEAN** for automatic provider retry). New exact code author no-provider suite **893 passed, 7 skipped**, 86.30% coverage; real `/var/tmp` durability-focused **22/22**. | **@agent-comms-ux** implementation/landing; **@pr1-compact-current-independent-review** independent reviewer. | Freeze/review **new** published runner/ACP/test bytes, including one-POST cap, Pi's retry/redirect/split-turn paths, fsync denial, orphan teardown and attached ACP session. Obtain mounted Toad positive/abort proof separately. An author suite is not a CLEAN verdict. |
| ACP provisional reply/goal-turn guard in `acp.py` and `tests/test_acp.py` published `ceb87f5`. Combined isolated suite **908 passed, 7 skipped**, 86.52% coverage; ACP/backend/compact **140/140**, Ruff/Black/mypy clean. It does **not** prove an ACKed queued steer was model-read. | **@agent-comms-ux** shared reconciler; **@pr1-acp-steer-disposition-fix** isolated per-input ID fix. | Require authoritative matching `inputId` user `message_start` for each forwarded/ACKed input. Reproduce the queued-steer no-start false-success witness without replay, merge exact isolated patch with ACP/compact hunks, then independent current-head check. |
| Nested native Pi launch durability: actual-d642 review reproduced `root/outer/inner` creation without syncing `root`; f31 historical 140-test log does not cover later goal-profile bytes. | **@pr1-native-ancestor-fsync-fix** isolated adapter/test owner; **@agent-comms-ux** integration. | Real `/var/tmp` ancestor fsync-denial-before-spawn tests, exact hashes, surgical shared merge; no review inheritance from f31 or moving detached root. |
| Usable persistent goals: two-file ledger-only CLEAN was frozen `0a821497…/8b4ae569…`, not approval for the combined path. Actual goal-profile probes saw duplicate `comms_goal` **two** localhost POSTs and unknown-tool **three** POSTs; legacy `tools._goal` ignores `AGENT_COMMS_GOAL_*`. Current combined reserve+claim can consume READY before the known-no-tools guard. | **@pr1-goal-coherent-integration-owner** sole isolated coherent-path writer; **@pr1-native-goal-tool-profile** isolated contract/regression; **@agent-comms-ux** shared landing. | Pre-reservation capability gate preserving goal/READY row and zero models on refusal; attempt-bound ledger/registry CAS and restricted real `comms_goal`, hard provider-generation cap, duplicate/unknown-tool proofs, authenticated one-shot human retry/cancel/clear. Independent whole-path verdict; keep tool-capable/paid execution OFF. |
| Default-OFF claim+announcement and opt-in N/K typed `NO_WAKE` are published, but scoped historical CLEAN findings do not approve the final combined bytes or paid use. | **@agent-comms-ux** coordinates exact-head claims/N/K review and final no-provider wiring. | Independently test whole-set conflicts, fsync/crash/rename/release/receipt authority and N/K selection through final native/ACP inputs. Paid proof needs its own enforceable spend ceiling and authorization; not a merge shortcut. |
| Core bounded image/resource input and compatible immutable Toad core pin/lockfile remain separate candidates. | **@mcp** isolated core image candidate; **@agent-comms-ux** core landing; Toad fork owner handles its own pin/lockfile. | Independent exact-byte image privacy/size/queued-input tests; pin both Toad dependency and lockfile to the reviewed compatible core, then actual ACP-to-widget proof. No base64 leakage or busy-prompt false success. |
| macOS 3.13 backend parser expected `current_prompt_input_missing`, saw `pi_invalid_rpc_event` in two cases on `9d2e828` (10/11 jobs passed); fail-closed is retained. **User explicitly deferred this portability fix as a merge priority**, not as a claim of green CI. | **@pr1-macos313-backend-ci-fix** parks isolated evidence for follow-up; **@agent-comms-ux** keeps red status visible and checks branch protection before merge. | Separate macOS-focused fix/CI follow-up. Do not falsify/disable evidence or silently override required checks. |
| Toad PR #1 feature behavior is **outside core PR #1**: false read-ACK, wrong-owner Back and sidebar/tab misalignment, blocked-goal recovery and mounted ACP startup require independent pinned proof. Textual `<16 ms` is a separate optimization. | **@toad-pr1-owner-nav-layout-fix** isolated navigation/layout patch; Toad fork feature owner retains Toad landing. | Mounted expanded/collapsed, owner-switch and read-ACK negative/positive tests on immutable Toad bytes; no edits to live dirty checkout. |

**Merge sequence:** publish individually scoped isolated fixes; verify each exact source/test manifest and no-provider negatives; reconcile ACP/native/goal/image into one final head; run complete Linux/Windows/quality plus supported-platform tests and independently review coherent claim/N/K/compact/goal bytes. The macOS 3.13 red job is an openly deferred follow-up at the user's direction; it does not waive provider-spend or input-authority defects. A required-status rule may still block the GitHub merge and must be handled explicitly, not bypassed invisibly. **Production activation remains OFF after merge.**
