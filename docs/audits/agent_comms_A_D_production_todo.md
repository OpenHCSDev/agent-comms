# Agent-comms A+D: production TODO

Checkpoint: **2026-09-23 20:00 UTC**, PR scope/status refreshed at head `34b9dcc783fa9bb9da17ec38c5f568ef266bb41d`. This is a production checklist for core N/K (**A**) and envelope resource claims (**D**), **not** a gate on merging passing, reviewed default-off experimental code. Toad functionality (**B**) and terminal presentation (**C**) remain separate fork tracks. Recheck GitHub head and source hashes before using this snapshot; newer backend/ACP corrections in the dirty development checkout are not included at this head.

## Already done — do not repeat to manufacture a gate

- [x] [Core PR #1](https://github.com/OpenHCSDev/agent-comms/pull/1) at `34b9dcc` includes the experimental same-process foreground recipient, explicit private sender, frozen N/selected K/typed `NO_WAKE`, D bus/pure transition and saved-route files, copied native-Pi patch, recovery, channel/collaboration/transcript/tool-diff and portability work. The historical N/K and stopped five-file D manifests were checked before integration; formatting and later changes mean their exact-byte reviews do **not** silently renew. Duplicate PR #2 is closed; PR #1 is open, ready for review, and unmerged. Its final-head CI/review still needs verification.
- [x] Current PR branch focused A+D+recovery offline smoke: **131/131 passed** (`tests/test_cohort_foreground.py`, `test_coordinated_runtime.py`, `test_envelope_claim_transitions.py`, `test_envelope_bus_integration.py`, and three recovery tests). Log `/var/tmp/ac-pr-A-D-recovery-smoke-20260923.log` SHA-256 `cd24d622127204e402eb59dd49c63139f57d7ed58f40b924faab099ef59dd556`. Foreground tests include actual two-recipient subprocess N2/K1, a typed `NO_WAKE`, wrong-PID/takeover denial and no replay after a failed model opportunity; their Pi boundary is faked. No paid call was made for these tests.
- [x] Historical supervised **real** GLM 5.3 Flash harness run covered N2/K2, N2/K1 and N1/K1 with four terminal selected claims, one `NO_WAKE`, five bound Pi responses. Log `/var/tmp/ac-real-nk-glm53-once-7ZWwCY.log` SHA prefix `3291b67e`; provider-side read-only observations are **not signed invoices**. This run did **not** exercise the new foreground command. **Do not rerun the harness.**
- [x] The previous D marker-rollback counterexample was reproduced against the corrected PR branch on 2026-09-23; it now exposes both durable rows and both claim owners. Log `/var/tmp/ac-D-marker-current-20260923.log` SHA-256 `cb06f4fc936590aa45ab1df19233665a0f2400c87586b7985ac46f12a9486193`. Historical five-file `a3652513…` remains NON-CLEAN. An independent reviewer subsequently returned **CLEAN** on the corrected, stopped default-off five-file D authority/read set (`declarations.py` `4e72d46d…`, `operations.py` `dc962eec…`, bus test `789d5630…`, pure transitions `408ea5cf…`, pure test `ceaef48e…`), with 34/34 focused tests (log SHA prefix `80430c7d`). This is scoped to those exact pre-format bytes, not a production cutover or raw-editor lock. Black formatting for PR integration changes current hashes; re-run integration tests on the final PR revision without presenting the prior exact-byte verdict as a new review.

## Remaining before **production A+D activation** (in dependency order)

1. [ ] **Provider spend/output fence:** implement and locally test a reviewed *enforced* per-request completion/output-token cap in the pinned copied Pi/adapter, plus a provider/account-side hard spend ceiling or equivalently enforced limit. The current timeout, existing Pi model `maxTokens=131072`, balance preflight and `--opt-in` are **not** a spend cap. No further paid test, user credential run, or widened root gate before this is proven. Keep `OPENROUTER_API_KEY=''` in offline tests. An uncertain provider opportunity is never automatically replayed.
2. [ ] **Supervised owner/cutover:** keep the existing fresh-recipient same-PID prepublication registration and duplicate-name rejection. Build/validate the production supervisor and an all-old-writers-stop barrier: inventory exact running PIDs/args/modules; resolve or mark in-flight attempts UNKNOWN; stop every legacy writer/child; prove quiescence; restart only the captured authorized set and verify identities/hashes. `restart --all` is **not** this barrier. Until proven, the foreground command remains explicit `/var/tmp` disposable opt-in only; never overwrite a live idle owner.
3. [ ] **Production D reader/cutover scope:** the corrected stopped five-file default-off D set already has a scoped independent CLEAN verdict; do not repeat its alias/marker correction review as a new gate. On one final unchanged integration revision, test any remaining production claim-bearing reader/recovery behavior and supervisor cutover beyond that scoped verdict. Preserve validated bus rows as authority (`bus_meta` only a hint), one whole-set CAS/no loser row and no false success after an uncertain append. Raw `bash`/edit write enforcement stays **ADVISORY**; do not advertise it as a resource-lock guarantee.
4. [ ] **New-command real-model and frozen integration:** only after #1, do one supervised bounded N2/K1 (and any remaining necessary N/K) through the *new* foreground/sender commands; verify input IDs/receipt, typed observer and no duplicate spend. Do **not** repeat old paid cases without a distinct proof need. Freeze one exact revision, run the full normalized suite there (not only current 131), and demonstrate operator disposition of UNKNOWN attempts and a supervised restart with no duplicate publication. Keep evidence private and redacted. Current `/var/tmp` disk free space is small; inspect capacity before large fixtures rather than deleting retained evidence.

**Switches:** production publication, monitoring, automatic retry and `SILENT` stay **OFF**. Turning any on is a separate explicit decision after its own evidence. An open/merged default-off code PR or passing focused suite is not such a decision.
